import React from "react";
import ReactDOM from "react-dom/client";
import ExamReady from "./ExamReady.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ExamReady />
  </React.StrictMode>
);
